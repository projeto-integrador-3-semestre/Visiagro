# Supabase

Execute estes arquivos no SQL Editor do Supabase:

1. `supabase-lavouras.sql`
2. `supabase-predictions-ativa.sql`

Eles criam ou ajustam as tabelas usadas pelo frontend e pelo backend: `profiles`, `pestes`, `agrotoxicos`, `predictions`, `lavouras`, `alertas_publicos` e `technical_reports`.

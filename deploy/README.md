# Deploy

Arquivos auxiliares de deploy ficam nesta pasta.

Os arquivos que ferramentas esperam na raiz continuam na raiz do projeto: `Dockerfile`, `docker-compose.yml`, `.dockerignore` e `docker-entrypoint.sh`.
